// useNotify.js
// 📌 Nivel 07 — Notification System (Hook)
//
// Implementa un hook que:
// - Consume el NotificationContext.
// - Retorna { notify } donde notify({ type, message }) agrega una notificación.
// - Tipos: 'success' | 'error' | 'warning' | 'info'
//
// ¡Haz que los tests pasen!

export function useNotify() {
    // Tu código aquí
}
