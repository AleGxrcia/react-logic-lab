// useDebounce.js
// 📌 Nivel 15 — Search Engine (Custom Hook)
//
// Implementa un hook useDebounce(value, delay) que:
// - Retorna el valor pasado, pero retrasado por `delay` ms.
// - Si el valor cambia antes de que pase el delay, reinicia el timer.
// - Usa useRef para el timeout ID y useEffect para el cleanup.
//
// Ejemplo:
//   const debouncedSearch = useDebounce(searchTerm, 300)
//   // debouncedSearch se actualiza 300ms después de que searchTerm deja de cambiar
//
// ¡Haz que los tests pasen!

export function useDebounce(value, delay) {
    // Tu código aquí
    return value
}
