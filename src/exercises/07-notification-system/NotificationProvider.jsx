// NotificationProvider.jsx
// 📌 Nivel 07 — Notification System (Compound Components)
//
// Implementa un NotificationProvider que:
// - Crea un Context para el sistema de notificaciones.
// - Mantiene un array de notificaciones con useReducer.
// - Renderiza la lista de notificaciones (role="alert", data-type="<type>").
// - Cada notificación tiene un botón de cerrar (aria-label="close" o "cerrar" o "dismiss").
// - Auto-dismiss cada notificación tras `autoDismissMs` milisegundos.
// - Limita las notificaciones visibles a `maxNotifications`.
// - Las notificaciones nuevas aparecen arriba (al inicio de la lista).
//
// Props del Provider:
//   - maxNotifications: number (default 5)
//   - autoDismissMs: number (default 3000)
//   - children: React nodes
//
// ¡Haz que los tests pasen!

export function NotificationProvider({ children }) {
    // Tu código aquí
    return null
}
