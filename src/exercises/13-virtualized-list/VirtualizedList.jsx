// VirtualizedList.jsx
// 📌 Nivel 13 — Virtualized List
//
// Implementa una lista de artículos con optimización de rendimiento:
// - Input de búsqueda (role="searchbox") para filtrar artículos.
// - Muestra el conteo de artículos visibles (data-testid="article-count").
// - Muestra "empty-state" (data-testid) si no hay resultados.
// - Usa `useMemo` para memoizar la lista filtrada.
// - Usa `useCallback` para estabilizar la referencia de `onBookmark`.
// - Renderiza cada artículo con `<ArticleItem>`.
//
// Props:
//   - articles: array de { id, title, summary, category, bookmarked }
//   - onBookmark: (id) => void
//
// ¡Haz que los tests pasen!

export default function VirtualizedList() {
    // Tu código aquí
    return null
}
